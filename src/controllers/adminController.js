const express = require('express');

const router = express.Router();

const User = require('../models/user');

router.post('/denuncia/:id', async (req, res) => {
    
})

module.exports = app => app.use(router);
